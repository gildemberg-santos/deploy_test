name: "CI"
on:
  push:
    branches: [ qa, main ]
  pull_request:
    branches: [ "*" ]
    types: [opened, synchronize, reopened]
jobs:
  build:
    name: "Build - ${{ github.event_name }}"
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:11-alpine
        ports:
          - "5432:5432"
        env:
          POSTGRES_DB: rails_test
          POSTGRES_USER: rails
          POSTGRES_PASSWORD: password
    env:
      RAILS_ENV: test
      DATABASE_URL: "postgres://rails:password@localhost:5432/rails_test"
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install Ruby and gems
        uses: ruby/setup-ruby@55283cc23133118229fd3f97f9336ee23a179fcf
        with:
          bundler-cache: true
      - name: Set up database structure
        run: bin/rails db:structure:load
      - name: Run tests
        run: bin/bundle exec rspec
      - name: Identificar Branch
        run: |
          if [[ "${{ github.ref }}" =~ ^refs/heads/qa_/ ]]; then
            echo "Esta é uma branch de qa"
          else
            echo "Esta não é uma branch de qa"
          fi
      - name: Identificar PR aberto
        run: |
          if [ "${{ github.event.pull_request.state }}" == "open" ]; then
            echo "A PR está aberta."
          else
            echo "A PR não está aberta."
          fi
  deploy:
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master
      - name: Deploy To Cloud66
        uses: cloud66-oss/github-actions@1.0.0
        with:
          CLOUD66_STACK: "my_stack_name"
          CLOUD66_TOKEN: ${{ secrets.CLOUD66_TOKEN }}
